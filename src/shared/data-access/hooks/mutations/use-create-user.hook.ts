// import { ApiService } from '@src/shared/services/api';
// import { User } from '@src/shared/services/api/types';
// import { AuthService } from '@src/shared/services/auth';
// import { useMutation } from 'react-query';

export const useCreateUser = () =>
  // email: string,
  // password: string,
  // successCallback?: (id: string) => void,
  {
    // return useMutation<string | undefined>(
    //   () => {
    //     // return ApiService.createUser(email, password);
    //     return AuthService.createUserWithEmailAndPassword(email, password);
    //   },
    //   {
    //     onSuccess: data => {
    //       data && successCallback!(data);
    //     },
    //   },
    // );
  };
